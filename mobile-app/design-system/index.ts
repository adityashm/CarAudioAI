import { tokens } from './tokens';

export { tokens, colors, typography, spacing, radius, shadows, color } from './tokens';
export type {
  DesignTokens,
  ColorTokens,
  TypographyTokens,
  SpacingTokens,
  RadiusTokens,
  ShadowTokens,
} from './tokens';

export default tokens;
